# Core runtime - deploys to a 512MB free tier.
# Requires EMBEDDING_BACKEND=pinecone (embeddings via Pinecone's hosted API).
#
# For local, offline embeddings install requirements-local.txt as well.

Flask==3.1.2
flask-cors==6.0.2
Flask-Limiter==4.1.1

python-dotenv==1.2.1
PyJWT==2.10.1

pymongo==4.16.0

groq==1.7.0
pinecone==8.1.0

pypdf==6.6.0
requests==2.32.5

gunicorn==21.2.0
